{
  "id": 19471,
  "title": "Parentheses",
  "time_limit": "1 초",
  "memory_limit": "512 MB",
  "description": "<p>A <em>correct parentheses sequence</em> can be defined recursively as follows:</p>\n<ul>\n<li>The empty string is a correct sequence.</li>\n<li>If $X$ and $Y$ are correct sequences, then $X Y$ (the concatenation of $X$ and $Y$) is a correct sequence.</li>\n<li>If $X$ is a correct sequence, then $<code>(</code>X<code>)</code>$ is a correct sequence.</li>\n</ul>\n<p>Each correct parentheses sequence can be derived using the above rules.</p>\n<p>For a parentheses sequence, you can make some operations with it.</p>\n<ul>\n<li>Each time you can choose two indices $L$ and $R$ such that $L \\le R$. The operation modifies the characters on indices from $L$ to $R$, inclusive.</li>\n<li>First, the order of these characters is reversed.</li>\n<li>Then, each character is toggled to the opposite one. That is, each '<code>(</code>' in the specified range changes to a '<code>)</code>' and vice versa.</li>\n</ul>\n<p>The <em>value</em> of a parentheses sequence is the minimal number of the operations required to change it into a correct parentheses sequence. If it is impossible, the value of the sequence is equal to $10^{100}$.</p>\n<p>For example, the value of \"<code>()((</code>\" is $1$, the value of \"<code>()()</code>\" is $0$, and the value of \"<code>(((</code>\" is $10^{100}$.</p>\n<p>You are given an integer $n$. For each $1 \\le i \\le n$, find the number $A_i$ of different parentheses sequence of length $n$ which has value $i$, and then calculate the sum $\\sum_{i = 0}^{n} ((i + 1) \\cdot A_i)$.</p>\n<p>The answer may be very large, so print it modulo the given integer $m$.</p>",
  "input": "<p>The first line of the input contains two integers $n$ and $m$ ($1 \\le n \\le 10^6$, $1 \\le m \\le 10^9$).</p>",
  "output": "<p>Print one integer: the answer to the problem.</p>",
  "samples": [
    {
      "input": "1 100\n",
      "output": "0\n"
    },
    {
      "input": "10 100\n",
      "output": "68\n"
    }
  ],
  "hint": null,
  "source": "Camp, Petrozavodsk Programming Camp, Summer 2016, Day 4: Ce Bin and Ryyi Ji Contest 1",
  "level": 21,
  "tags": ["math", "number_theory", "combinatorics"],
  "accepted_user_count": 10,
  "submission_count": null,
  "average_tries": 1.7999999523162842
}
