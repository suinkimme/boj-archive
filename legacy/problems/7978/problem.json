{
  "id": 7978,
  "title": "Evaluation of an Expression",
  "time_limit": "1 초",
  "memory_limit": "128 MB",
  "description": "<p>Consider an expression E, containing integer constants from 0 to 9, variables from a to z, and the following operations: addition, multiplication and exponentiation with a constant exponent. Quite surprisingly, each of the variables a, b, . . . , z appears in the expression E at most once. For a given prime number p, we would like to know how many roots modulo p the polynomial represented by this expression has. In other words, we want to count the number of ways in which integers from 0 to p − 1 can be assigned to the variables in E, so that the value of E is divisible by p. Since the number of such roots can turn out large, it suffices to output it modulo 30 011.</p>\n<p>For example, the polynomial represented by the following expression:</p>\n<p style=\"text-align: center;\">E = ((a + y) · (z + 8))<sup>2</sup></p>\n<p>has 15 roots modulo p = 3, among which the roots:</p>\n<p style=\"text-align: center;\">(a = 0; y = 0; z = 0); (a = 1; y = 2; z = 0); (a = 2; y = 0; z = 1)</p>\n<p>can be found.</p>\n<p>More formally, an expression is defined as follows:</p>\n<ul>\n<li>Each integer constant 0, 1, . . . , 9 is an expression.</li>\n<li>Each variable a, b, . . . , z is an expression.</li>\n<li>If A and B are any expressions, then each of (A+B) and (A*B) is also an expression: the first is the sum</li>\n<li>of expressions A and B, and the second is their product.</li>\n<li>If A is any expression, and B is an integer constant from 2, 3, . . . , 9, then (A^B) is also an expression: the expression A raised to the power of B.</li>\n</ul>",
  "input": "<p>The first line of input contains one prime number p (2 ≤ p &lt; 15 000). The second line contains an expression E as specified above, described by a sequence of at most 300 characters 0, 1, . . . , 9, a, b, . . . , z, +, *, ^, (, ), without any white space.</p>",
  "output": "<p>Let k denote the number of roots modulo p of the polynomial E. Your program should output one non-negative integer, k mod 30 011.</p>",
  "samples": [
    {
      "input": "3\n(((a+y)*(z+8))^2)\n",
      "output": "15\n"
    }
  ],
  "hint": null,
  "source": "ICPC, Regionals, Europe, Central European Regional Contest, Poland Collegiate Programming Contest, AMPPZ 2012",
  "level": 24,
  "tags": [
    "math",
    "implementation",
    "dp",
    "string",
    "trees",
    "number_theory",
    "dp_tree",
    "parsing",
    "fft",
    "discrete_log"
  ],
  "accepted_user_count": 2,
  "submission_count": null,
  "average_tries": 1
}
