{
  "id": 23115,
  "title": "Multiple Parentheses",
  "time_limit": "2 초",
  "memory_limit": "512 MB",
  "description": "<p>Consider strings consisting of the brackets '<code>(</code>' and '<code>)</code>'. </p>\n<p>The regular bracket sequences are the strings which can be obtained by the following rules:</p>\n<ul>\n<li>Empty string is a regular bracket sequence.</li>\n<li>If $A$ is a regular bracket sequence, then <code>(</code>$A$<code>)</code> is a regular bracket sequence.</li>\n<li>If $A$ and $B$ are regular bracket sequences, then the concatenation of $A$ and $B$ is a regular bracket sequence.</li>\n</ul>\n<p>You are given $N$ boxes numbered $1, 2, \\ldots, N$, and also two integers, $M$ and $K$. Your task is to put exactly one regular bracket sequence in each of $N$ boxes such that the following conditions are met:</p>\n<ul>\n<li>The total number of '<code>(</code>' brackets in all $N$ boxes is equal to $M$.</li>\n<li>The regular bracket sequences of length $2 \\cdot K$ cannot be put into the boxes.</li>\n</ul>\n<p>Count the number of different ways to do that. Two distributions are considered different if there exists a number $i$ such that box $i$ contains different regular bracket sequences in those distributions.</p>\n<p>Because the answer may be very large, print the answer modulo $998\\,244\\,353$.</p>",
  "input": "<p>The input contains one line with three integers $N$, $M$, and $K$ in it ($1 \\le M, N \\le 10^6$, $1 \\le K \\le M$).</p>",
  "output": "<p>Print the answer modulo $998\\,244\\,353$.</p>",
  "samples": [
    {
      "input": "2 2 1\n",
      "output": "4\n"
    },
    {
      "input": "1 1 1\n",
      "output": "0\n"
    },
    {
      "input": "24 120 30\n",
      "output": "379268651\n"
    }
  ],
  "hint": "<p>For the first example, the following distributions meet the conditions:</p>\n<ul>\n<li><code>(())</code>, empty;</li>\n<li><code>()()</code>, empty;</li>\n<li>empty, <code>(())</code>;</li>\n<li>empty, <code>()()</code>.</li>\n</ul>\n<p>So, the answer is $4$.</p>",
  "source": "Camp, Petrozavodsk Programming Camp, Summer 2021, Day 1: Kyoto U Contest 1",
  "level": 26,
  "tags": [
    "math",
    "number_theory",
    "combinatorics",
    "inclusion_and_exclusion",
    "modular_multiplicative_inverse"
  ],
  "accepted_user_count": 18,
  "submission_count": null,
  "average_tries": 1.7222000360488892
}
