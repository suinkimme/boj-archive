{
  "id": 8105,
  "title": "Gambling",
  "time_limit": "1 초",
  "memory_limit": "128 MB",
  "description": "<p>A gambling machine consists of n generators of integers: G<sub>1</sub>, ..., G<sub>n</sub>, where 1 ≤ n ≤ 1,000. The generator G<sub>i</sub> can generate integers only from the certain set Si ⊆ {1, ..., n}, or 0 which means that the game is over. The S<sub>i</sub> can be an empty set. Let k<sub>i</sub> be the number of elements of the set S<sub>i</sub>. The sum of all the integers k<sub>i</sub>, for i = 1, ..., n, cannot exceed 12,000.</p>\n<p>While G<sub>i</sub> is activated for the first time it generates an integer from the set Si. The next activating ends up in generating an integer from the set S<sub>i</sub>, which was not chosen before. If there is no such an integer, G<sub>i</sub> generates zero.</p>\n<p>The machine starts with activating G<sub>1</sub>. Then the generators are activated according to the following rule: In case when a generator generated a positive integer r, the next activated generator is G<sub>r</sub>. If zero is generated the machine stops.</p>\n<p>The machine looses if the generator G<sub>n</sub> generates zero and the rest of the generators had exhausted their sets of integers. The machine is well constructed if it may generate a sequence of integers ending with zero, but not leading to a defeat.</p>\n<p>Write a program that:</p>\n<ul>\n<li>reads from the standard input the description of the machine, i.e. the number of generators n, integers k<sub>i</sub> and sets S<sub>i</sub>,</li>\n<li>verifies, whether the machine described in the input file is well constructed,</li>\n<li>if it is well constructed, writes to the standard output a sequence of integers, which may be generated by the machine, is ended with zero, and does not lead to defeat; if not, writes one word <code>NIE</code> (which means “no” in Polish).</li>\n</ul>",
  "input": "<p>In the first line of the standard input there is written one positive integer n, n ≤ 1,000. This is the number of generators. In the (i+1)-st line (for i = 1, ..., n) there is written an integer k<sub>i</sub> followed by all the elements of the set S<sub>i</sub> (written in arbitrary order). The integers in each line are separated by single spaces.</p>",
  "output": "<p>In the standard output there should be written one word <code>NIE</code> (if the machine isn't well constructed) or one line containing an appropriate finite series of integers separated by single spaces.</p>",
  "samples": [
    {
      "input": "2\n2 1 2\n1 2\n",
      "output": "2 2 0\n"
    }
  ],
  "hint": null,
  "source": "Olympiad, Polish Olympiad in Informatics, POI 1995/1996, Stage 2",
  "level": 0,
  "tags": [],
  "accepted_user_count": 0,
  "submission_count": null,
  "average_tries": 0
}