{
  "id": 25355,
  "title": "Admissible Map",
  "time_limit": "3 초",
  "memory_limit": "512 MB",
  "description": "<p>A <em>map</em> is a matrix consisting of symbols from the set of '<code>U</code>', '<code>L</code>', '<code>D</code>', and '<code>R</code>'.</p>\n<p>A <em>map graph</em> of a map matrix $a$ is a directed graph with $n \\cdot m$ vertices numbered as $(i, j)$ ($1 \\le i \\le n; 1 \\le j \\le m$), where $n$ is the number of rows in the matrix, $m$ is the number of columns in the matrix. The graph has $n \\cdot m$ directed edges $(i, j) \\to (i + di_{a_{i, j}}, j + dj_{a_{i, j}})$, where $(di_U, dj_U) = (-1, 0)$; $(di_L, dj_L) = (0, -1)$; $(di_D, dj_D) = (1, 0)$; $(di_R, dj_R) = (0, 1)$. A map graph is <em>valid</em> when all edges point to valid vertices in the graph.</p>\n<p>An <em>admissible map</em> is a map such that its map graph is valid and consists of a set of cycles.</p>\n<p>A <em>description</em> of a map $a$ is a concatenation of all rows of the map --- a string $a_{1,1} a_{1,2} \\ldots a_{1, m} a_{2, 1} \\ldots a_{n, m}$.</p>\n<p>You are given a string $s$. Your task is to find how many substrings of this string can constitute a description of some admissible map.</p>\n<p>A <em>substring</em> of a string $s_1s_2 \\ldots s_l$ of length $l$ is defined by a pair of indices $p$ and $q$ ($1 \\le p \\le q \\le l$) and is equal to $s_ps_{p+1} \\ldots s_q$. Two substrings of $s$ are considered different when the pair of their indices $(p, q)$ differs, even if they represent the same resulting string.</p>",
  "input": "<p>In the only input line, there is a string $s$, consisting of at least one and at most $20\\,000$ symbols '<code>U</code>', '<code>L</code>', '<code>D</code>', or '<code>R</code>'.</p>",
  "output": "<p>Output one integer --- the number of substrings of $s$ that constitute a description of some admissible map.</p>",
  "samples": [
    {
      "input": "RDUL\n",
      "output": "2\n"
    },
    {
      "input": "RDRU\n",
      "output": "0\n"
    },
    {
      "input": "RLRLRL\n",
      "output": "6\n"
    }
  ],
  "hint": "<p>In the first example, there are two substrings that can constitute a description of an admissible map --- \"<code>RDUL</code>\" as a matrix of size $2 \\times 2$ (pic. 1) and \"<code>DU</code>\" as a matrix of size $2 \\times 1$ (pic. 2).</p>\n<p>In the second example, no substring can constitute a description of an admissible map. E. g. if we try to look at the string \"<code>RDRU</code>\" as a matrix of size $2 \\times 2$, we can find out that the resulting graph is not a set of cycles (pic. 3).</p>\n<p>In the third example, three substrings \"<code>RL</code>\", two substrings \"<code>RLRL</code>\" and one substring \"<code>RLRLRL</code>\" can constitute an admissible map, some of them in multiple ways. E. g. here are two illustrations of substring \"<code>RLRLRL</code>\" as matrices of size $3 \\times 2$ (pic. 4) and $1 \\times 6$ (pic. 5).</p>\n<p style=\"text-align: center;\"><img alt=\"\" src=\"1.png\" style=\"width: 103px; height: 122px;\"/> <img alt=\"\" src=\"2.png\" style=\"height: 122px; width: 53px;\"/> <img alt=\"\" src=\"3.png\" style=\"width: 103px; height: 122px;\"/> <img alt=\"\" src=\"4.png\" style=\"width: 103px; height: 174px;\"/> <img alt=\"\" src=\"5.png\" style=\"width: 303px; height: 72px;\"/></p>",
  "source": "ICPC, Regionals, Northern Eurasia, Northern Eurasia Finals, Northern Eurasia Finals 2021",
  "level": 24,
  "tags": ["ad_hoc", "dp", "functional_graph", "graphs", "hashing"],
  "accepted_user_count": 9,
  "submission_count": null,
  "average_tries": 3.6666998863220215
}
