{
  "id": 8109,
  "title": "Word Equalizing",
  "time_limit": "1 초",
  "memory_limit": "128 MB",
  "description": "<p>There are given two words x, y and finite series of words (w<sub>1</sub>, w<sub>2</sub>, ..., w<sub>k</sub>). An operation w ⊕w<sub>i</sub> means a connection (concatenation) of word w with another word w<sub>i</sub> (1 ≤ i ≤ k), i.e. is writing a word w<sub>i</sub> just after the word w.</p>\n<p>We want to verify if the words x and y can be equalized with words from the given series. The question is whether it is possible to extend the words x and y with words from the series in order to produce two identical words.</p>\n<p>The words <code>abba</code> and <code>ab</code> can be equalized with the words from the series: <code>baaabad aa badccaa cc</code>. In this purpose to the word <code>abba</code> we should add: <code>aa</code> and <code>badccaa</code>, and to the word <code>ab</code> — firstly <code>baaabad</code>, then <code>cc</code>, and finally <code>aa</code>. In both cases we result in the same word: <code>abbaaabadccaa</code>.</p>\n<p>Write a program that:</p>\n<ul>\n<li>reads from the standard input a length k of a given series of words, then descriptions of two words x and y as well as descriptions of the words from the series: (w<sub>1</sub>, w<sub>2</sub>, ..., w<sub>k</sub>),</li>\n<li>verifies whether the words x and y can be equalized with the words from the given series; if it is possible it finds the minimal number of operations ⊕, which are necessary,</li>\n<li>write this number to the standard output.</li>\n</ul>",
  "input": "<p>In the first line of the standard input there is written a positive integer k, k ≤ 40. This is the length of the series (w<sub>1</sub>, w<sub>2</sub>, ..., w<sub>k</sub>). In the second and the third line there are descriptions of the words x and y. In the following k lines there are descriptions of the succeeding words in the series (w<sub>1</sub>, w<sub>2</sub>, ..., w<sub>k</sub>)) — each description in a separate line. A description of the word consists of a natural number, which is the length of the word, and a word itself written as a series of characters. The number and the word are separated by a single space. Each word consists only of small English letters from <code>a</code> to <code>z</code> and its length is not greater than 2,000. The sum of lengths of all given words is not greater than 5,000.</p>",
  "output": "<p>To the standard output there should be written:</p>\n<ul>\n<li>one nonnegative integer, the minimal number of operations ⊕, which are necessary to equalize given words x and y,</li>\n<li>or the word <code>NIE</code> (which means “no” in Polish), if it is not possible to equalize the words.</li>\n</ul>",
  "samples": [
    {
      "input": "4\n4 abba\n2 ab\n7 baaabad\n2 aa\n7 badccaa\n2 cc\n",
      "output": "5\n"
    },
    {
      "input": "4\n1 a\n2 ab\n2 bb\n2 ab\n2 ba\n2 aa\n",
      "output": "NIE\n"
    }
  ],
  "hint": null,
  "source": "Olympiad, Polish Olympiad in Informatics, POI 1995/1996, Stage 3",
  "level": 19,
  "tags": [
    "graphs",
    "data_structures",
    "string",
    "graph_traversal",
    "set",
    "bfs",
    "shortest_path",
    "hashing"
  ],
  "accepted_user_count": 17,
  "submission_count": null,
  "average_tries": 1.2352999448776245
}