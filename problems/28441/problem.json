{
  "id": 28441,
  "title": "Ancient Machine 2",
  "time_limit": "2 초",
  "memory_limit": "1024 MB",
  "description": "<p>Bitaro and Bibako are archaeologists who excavated and investigated the ruin of JOI Kingdom. In the ruin, Bitaro found an old stone slate, and Bibako found an old machine.</p>\n<p>From the results of the study, Bitaro found out that a string $S$ of length $N$ was written on the stone slate. Each character of the string is either ‘<code>0</code>’ or ‘<code>1</code>’. However, he does not yet know each character of the string $S$.</p>\n<p>On the other hand, from the results of the study, Bibako found out how to use the machine. To use it, we put the stone slate on the machine, input an integer $m$ and two sequences of integers $a$, $b$, and make a query. Here the integer $m$ and the sequences of integers $a$, $b$ should satisfy the following conditions:</p>\n<ul>\n<li>The integer $m$ is between $1$ and $1\\,002$, inclusive.</li>\n<li>Both of the lengths of the sequences $a$, $b$ are equal to $m$.</li>\n<li>Every element of the sequences $a$, $b$ is an integer between $0$ and $m - 1$, inclusive.</li>\n</ul>\n<p>If we put the stone slate on the machine, input an integer $m$ and two sequences of integers $a$, $b$, and make a query, the machine will operate as follows and will show an integer.</p>\n<ol>\n<li>The machine sets $0$ in the <strong>memory area</strong> of the machine.</li>\n<li>The machine performs the following $N$ <strong>operations</strong>. The $(i + 1)$-th ($0 ≤ i ≤ N - 1$) operation proceeds as follows.\n\t<ul>\n<li>Let $x$ be the current integer set in the memory area of the machine. The machine reads the character $S_i$. Here, $S_i$ is the $i$-th character of the string $S$, if we count the characters of the string $S$ so that the first character is the $0$-th character.\n\t\t<ul>\n<li>If $S_i$ is ‘<code>0</code>’, the machine sets $a_x$ in the memory area of the machine. Here, $a_x$ is the $x$-th element of the sequence $a$, if we count the elements of the sequence $a$ so that the first element is the $0$-th element.</li>\n<li>If $S_i$ is ‘<code>1</code>’, the machine sets $b_x$ in the memory area of the machine. Here, $b_x$ is the $x$-th element of the sequence $b$, if we count the elements of the sequence $b$ so that the first element is the $0$-th element.</li>\n</ul>\n</li>\n</ul>\n</li>\n<li>The machine shows the integer which is finally set in the memory area.</li>\n</ol>\n<p>Using the machine, Bitaro wants to specify the string written on the stone slate. However, since the machine is very fragile, the number of queries cannot exceed $1\\,000$. Moreover, the maximum of the integer $m$ input to the machine for a query should be as small as possible.</p>\n<p>Write a program which, using the machine, specifies the string written on the stone slate.</p>",
  "input": "",
  "output": "",
  "samples": [],
  "hint": null,
  "source": ", Contest, JOI Open Contest, JOI Open Contest 2023",
  "level": 26,
  "tags": [
    "math",
    "string",
    "ad_hoc",
    "constructive",
    "linear_algebra",
    "kmp",
    "gaussian_elimination"
  ],
  "accepted_user_count": 7,
  "submission_count": null,
  "average_tries": 1.7143000364303589
}