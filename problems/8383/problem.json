{
  "id": 8383,
  "title": "Almost Conjugates",
  "time_limit": "1 초",
  "memory_limit": "128 MB",
  "description": "<p>Strictly thinking, the word <i>almost</i> is equivalent to the word <i>no</i>. On the other hand, these words are not exactly synonyms. More precisely, the word <i>almost</i> is closer in meaning to the word <i>yes</i> than to the word <i>no</i>.</p>\n<p>We say that two words are <i>conjugates</i> if we can modify the first word to obtain the second one, by repeatedly moving one letter from the beginning of the first word to its end. For instance, words <code>ababa</code> and <code>abaab</code> are conjugates, but words <code>ababa</code> and <code>baaab</code> are not. We say that two words are <i>almost conjugates</i> if:</p>\n<ul>\n<li>they are not conjugates and</li>\n<li>by repeatedly moving one letter from the beginning of the first word to its end we obtain a word that differs from the second word at exactly one position.</li>\n</ul>\n<p>For instance, words <code>ababa</code> and <code>aaaab</code> are almost conjugates, but words <code>ababa</code> and <code>bbbbb</code> are not.</p>\n<p>Write a program which:</p>\n<ul>\n<li>reads two words from the standard input,</li>\n<li>checks whether they are almost conjugates, and if so then proves it,</li>\n<li>writes the result to the standard output.</li>\n</ul>",
  "input": "<p>The first line of input contains one integer <em>n</em> (1 ≤ <em>n</em> ≤ 1 000 000), representing the length of each of the words. The second line contains the first word and the third line contains the second word. Each word is a sequence of <em>n</em> small letters of English alphabet.</p>",
  "output": "<p>The first line of output should contain one word - <code>TAK</code> (i.e. <i>yes</i> in Polish) if the words from the input are almost conjugates or <code>NIE</code> (i.e. <i>no</i> in Polish) otherwise. If the first line contains the word <code>TAK</code>, then the second line should contain an increasing sequence of non-negative integers, each from the range [0,<em>n</em> - 1], separated with single spaces. The sequence should contain all possible numbers of operations of moving one letter from the beginning of the first word to its end, after which the resulting word differs from the second word at exactly one position.</p>",
  "samples": [
    {
      "input": "5\nababa\naaaab\n",
      "output": "TAK\n2 4\n"
    },
    {
      "input": "5\nababa\nbbbbb\n",
      "output": "NIE\n"
    }
  ],
  "hint": null,
  "source": "Contest, Algorithmic Engagements, PA 2007",
  "level": 0,
  "tags": [],
  "accepted_user_count": 0,
  "submission_count": null,
  "average_tries": 0
}
