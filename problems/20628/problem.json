{
  "id": 20628,
  "title": "Parity Sort",
  "time_limit": "2 초",
  "memory_limit": "512 MB",
  "description": "<p>You have a permutation $P$ of length $n$. In this problem, elements of the permutation are integers from $0$ to $n - 1$.</p>\n<p>Your task is to perform the following operation up to $30$ times to sort $P$ in ascending order.</p>\n<p>The operation is defined by two parameters: an integer $t$ denoting the mode of operation ($0 \\le t \\le 1$) and a string $S$ of length $n$, consisting of <code>0</code>s and <code>1</code>s.</p>\n<p>At the start of the process, we have two empty sequences, $A$ and $B$.</p>\n<p>Next, for each $i$ from $1$ to $n$, we repeat the following step:</p>\n<ul>\n<li>If $S_i = 0$, do nothing.</li>\n<li>If $S_i = 1$: if $P_i$ is even, add $P_i$ to the end of sequence $A$, otherwise add $P_i$ to the end of sequence $B$.</li>\n</ul>\n<p>If $t = 0$, sequence $C$ is the concatenation of sequence $A$ and sequence $B$ in that order.</p>\n<p>If $t = 1$, sequence $C$ is the concatenation of sequence $B$ and sequence $A$ in that order.</p>\n<p>Next, for each $i$ fron $1$ to $n$, we repeat the following step:</p>\n<ul>\n<li>If $S_i = 0$, do nothing.</li>\n<li>If $S_i = 1$, replace $P_i$ with the first element of $C$ and erase the first element of $C$.</li>\n</ul>\n<p>For example, if $n = 7$, $P = \\{0, 4, 2, 3, 6, 5, 1\\}$ and we choose $t = 1$ and $S = <code>1101101</code>$, the process is shown on the picture below.</p>\n<p style=\"text-align: center;\"><img alt=\"\" src=\"1.png\"/></p>",
  "input": "<p>The first line of the input contains one integer $n$ ($1 \\le n \\le 15\\,000$). The second line contains $n$ integers $P_i$ ($0 \\le P_i \\le n - 1$, $P_i \\ne P_j$ if $i \\ne j$).</p>",
  "output": "<p>Print one of the possible sorting sequences in the following format:</p>\n<p>On the first line, print one integer $k$: the number of operations ($0 \\le k \\le 30$).</p>\n<p>The $i$-th of the following $k$ lines shall describe the $i$-th operation and contain integer $t_i$ ($0 \\le t_i \\le 1$) and binary string $S$ of length $n$.</p>\n<p>If there is more than one such sequence, choose any one of them. Note that you don't need to minimize $k$.</p>",
  "samples": [
    {
      "input": "7\n0 4 2 3 6 5 1\n",
      "output": "1\n1 0100101\n"
    }
  ],
  "hint": null,
  "source": "Camp, Petrozavodsk Programming Camp, Summer 2020, Day 5: JAG Summer+ Opening Contest",
  "level": 22,
  "tags": [
    "sorting",
    "constructive"
  ],
  "accepted_user_count": 3,
  "submission_count": null,
  "average_tries": 1.666700005531311
}