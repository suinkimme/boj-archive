{
  "id": 17437,
  "title": "Source",
  "time_limit": "0.07 초",
  "memory_limit": "16 MB",
  "description": "<p>An important problem of software maintenance is to track down duplication in a large software system. One would like to find not only exact matches between sections of code, but parameterized matches, where a parameterized match between two sections of code means that one section can be transformed into the other by replacing the parameter names (e.g., identifiers and constants) of one section by the parameter names of the other via a one-to-one (bijective) function.</p>\n<p>Let Σ and Π be two alphabets: Σ is the upper case English alphabet and Π is the lower case English alphabet. Each symbol in Σ represents a token and each symbol in Π represents a parameter.</p>\n<p>A string can consist of any combinations of tokens and parameters from Σ and Π. Two strings A and B are said to p-match if and only if</p>\n<ol>\n<li>A and B have the same length (more formally: length(A) = length(B))</li>\n<li>Each token in A matches a token in B and each parameter in A matches a parameter in B (more formally: (A<sub>i</sub> is a token and B<sub>i</sub> is a token) or (A<sub>i</sub> is a parameter and B<sub>i</sub> is a parameter) for any 1 ≤ i ≤ length(A))</li>\n<li>The alignment of tokens in A and B is a perfect match (more formally: if A<sub>i</sub> is a token then A<sub>i</sub> = B<sub>i</sub> for any 1 ≤ i ≤ length(A))</li>\n<li>The alignment of parameters in A and B defines a one-to-one correspondence between parameter names in A and parameter names in B (more formally: there exists a one-to-one (bijective) function f: Π → Π such that if A<sub>i</sub> is a parameter f(A<sub>i</sub>) = B<sub>i</sub> for any 1 ≤ i ≤ length(A))</li>\n</ol>\n<p>A token represents a part of the program that cannot be changed, whereas a parameter represents a program's variable, which can be renamed as long as all occurrences of the variable are renamed consistently. Thus if A and B p-match, then the variable names in A could be changed to the corresponding variable names in B, making the two programs identical. If these two problems were part of a larger program, then they could both be replaced by a call to a single subroutine.</p>\n<p>Given a text T and a pattern P, each a string over Σ and Π, find all substrings of T that p-match P.</p>",
  "input": "<p>The first line of the input contains the string P followed by the second line containing the string T.</p>",
  "output": "<p>The output must contain on the first line the number of substrings of T that p-match P. On the second line the offsets of those substrings are to be printed separated by a space (by convention, the prefixes of T have offset 1).</p>",
  "samples": [
    {
      "input": "XYabCaCXZddbW\nxXYdxCdCXZccxW\n",
      "output": "1\n2\n"
    }
  ],
  "hint": "<p>The pattern <code>XYabCaCXZddbW</code> p-matchs the 2-offset text substring <code>XYdxCdCXZccxW</code> but does not pmatch the 1-offset text substring <code>xXYdxCdCXZccx</code>.</p>\n<p>Notice that when the p-match occurs, the following one-to-one (bijective) function f is used:</p>\n<ul>\n<li>f(<code>a</code>) = <code>d</code></li>\n<li>f(<code>b</code>) = <code>x</code></li>\n<li>f(<code>d</code>) = <code>c</code></li>\n</ul>",
  "source": "Olympiad, Romanian Master of Informatics, Romanian Master of Informatics 2014",
  "level": 0,
  "tags": [],
  "accepted_user_count": 0,
  "submission_count": null,
  "average_tries": 0
}