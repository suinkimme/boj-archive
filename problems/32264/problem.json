{
  "id": 32264,
  "title": "Tree",
  "time_limit": "2 초",
  "memory_limit": "1024 MB",
  "description": "<p>Consider a <strong>tree</strong> consisting of $N$ <strong>vertices</strong>, numbered from $0$ to $N - 1$. Vertex $0$ is called the <strong>root</strong>. Every vertex, except for the root, has a single <strong>parent</strong>. For every $i$, such that $1 ≤ i &lt; N$, the parent of vertex $i$ is vertex $P[i]$, where $P[i] &lt; i$. We also assume $P[0] = -1$.</p>\n<p>For any vertex $i$ ($0 ≤ i &lt; N$), the <strong>subtree</strong> of $i$ is the set of the following vertices:</p>\n<ul>\n<li>$i$,</li>\n<li>and any vertex whose parent is $i$,</li>\n<li>and any vertex whose parent's parent is $i$,</li>\n<li>and any vertex whose parent's parent's parent is $i$, and etc.</li>\n</ul>\n<p>The picture below shows an example tree consisting of $N = 6$ vertices. Each arrow connects a vertex to its parent, except for the root, which has no parent. The subtree of vertex $2$ contains vertices $2$, $3$, $4$ and $5$. The subtree of vertex $0$ contains all $6$ vertices of the tree and the subtree of vertex $4$ contains only vertex $4$.</p>\n<p style=\"text-align: center;\"><img alt=\"\" src=\"1.png\" style=\"width: 183px; height: 200px;\"/></p>\n<p>Each vertex is assigned a nonnegative integer <strong>weight</strong>. We denote the weight of vertex $i$ ($0 ≤ i &lt; N$) by $W[i]$.</p>\n<p>Your task is to write a program that will answer $Q$ queries, each specified by a pair of positive integers $(L,R)$. The answer to the query should be computed as follows.</p>\n<p>Consider assigning an integer, called a <strong>coefficient</strong>, to each vertex of the tree. Such an assignment is described by a sequence $C[0],\\dots ,C[N - 1]$, where $C[i]$ ($0 ≤ i &lt; N$) is the coefficient assigned to vertex $i$. Let us call this sequence a <strong>coefficient sequence</strong>. Note that the elements of the coefficient sequence can be negative, $0$, or positive.</p>\n<p>For a query $(L,R)$, a coefficient sequence is called <strong>valid</strong> if, for every vertex $i$ ($0 ≤ i &lt; N$), the following condition holds: the sum of the coefficients of the vertices in the subtree of vertex $i$ is not less than $L$ and not greater than $R$.</p>\n<p>For a given coefficient sequence $C[0],\\dots ,C[N - 1]$, the <strong>cost</strong> of a vertex $i$ is $|C[i]| \\cdot W[i]$, where $|C[i]|$ denotes the absolute value of $C[i]$. Finally, the <strong>total cost</strong> is the sum of the costs of all vertices. Your task is to compute, for each query, the <strong>minimum total cost</strong> that can be attained by some valid coefficient sequence.</p>\n<p>It can be shown that for any query, at least one valid coefficient sequence exists.</p>",
  "input": "",
  "output": "",
  "samples": [],
  "hint": null,
  "source": ", Olympiad, International Olympiad in Informatics, IOI 2024, Day 1",
  "level": 25,
  "tags": ["data_structures", "greedy", "trees", "prefix_sum", "disjoint_set"],
  "accepted_user_count": 17,
  "submission_count": null,
  "average_tries": 3.764699935913086
}
